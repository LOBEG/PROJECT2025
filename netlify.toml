# =============================================================================
# FINAL CORRECTED NETLIFY CONFIGURATION (v2)
# Reverting the publish directory to "dist" to fix the build error.
# =============================================================================

[build]
  # ✅ FIX: Your project's build command creates a "dist" directory.
  # Reverting this to "dist" will fix the build failure.
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  external_node_modules = ["@upstash/redis"]

[build.environment]
  # Correct Client ID for the build environment
  MICROSOFT_CLIENT_ID = "2e338732-c914-4129-a148-45c24f2da81d"
  MICROSOFT_TENANT_ID = "common"
  MICROSOFT_CLIENT_SECRET = "" 
  UPSTASH_REDIS_REST_URL = "placeholder-optional-redis-url"
  UPSTASH_REDIS_REST_TOKEN = "placeholder-optional-redis-token"

# --- Redirect and Rewrite Rules ---

# Rule 1: API Function Proxy
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true

# Rule 2: Single-Page Application (SPA) Fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
